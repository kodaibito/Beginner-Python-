# Ask the user to input coordinates
x = float(input("Enter x coordinate: "))
y = float(input("Enter y coordinate: "))

# Circle centered at (0,0) with radius 5
radius = 5

# Check if the point lies inside the circle
# A point (x, y) is inside if x^2 + y^2 <= radius^2
if x**2 + y**2 <= radius**2:
    print("inside")
else:
    print("outside")